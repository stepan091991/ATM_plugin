Minecraft ATM machine plugin.
- The ATM interface can only be opened with the `/atm` or` /bank` command, requires `atm.open` permission.
- You can reload the configuration using command `/atm reload`, requires `atm.reload` permission.
